# 近期修改摘要

## 已完成的工作
- **碰撞检测/解算覆盖**：
  - `PhysicsSystem::BasicCollisionDetection` 现遍历对象对，调用 `CollisionDetection::ObjectIntersection`，对命中立即调用 `ImpulseResolveCollision` 并登记碰撞。
  - `PhysicsSystem::ImpulseResolveCollision` 实现默认反弹（e=0.8）、速度沿法线冲量、穿透位置校正；静态对静态直接返回。
  - 现有 `CollisionDetection` 中 AABB/OBB/Sphere/Capsule 射线与体积检测、体积间碰撞已可工作（OBB vs OBB 近似为世界 AABB）。
- **玩家移动**：相机朝向力控制、跳跃（接地射线判定）、地/空摩擦、速度上限。
- **物理积分**：力/重力/扭矩→速度/位置/朝向，含阻尼；重力可切换。
- **文档**：`project_context/上下文管理.md` 更新全局状态；`ProjectPlan.md` 在仓库。

## 当前风险/待办
- OBB-OBB 碰撞为近似 AABB，后续可换 SAT 提升精度。
- 未实现 BroadPhase；大场景可能性能不足，可后续启用四叉树/包围盒更新。
- 需进一步验证穿模/弹性参数，调整 `restitution`、穿透修正比例。

## 受影响的主要文件
- `CSC8503CoreClasses/PhysicsSystem.cpp`
- `CSC8503CoreClasses/CollisionDetection.cpp`
- `CSC8503/TutorialGame.cpp` / `.h`
