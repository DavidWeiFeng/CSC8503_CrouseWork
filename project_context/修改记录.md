# 近期修改摘要

## 已完成的工作
- **玩家刚体移动**：在 `CSC8503/TutorialGame.cpp` 中新增 `HandlePlayerMovement`，实现相机朝向的 WASD 力控制、速度上限和玩家引用管理；初始化时记录 `playerObject`，重置世界时清理引用。
- **物理积分启用**：在 `CSC8503CoreClasses/PhysicsSystem.cpp` 中实现力/重力/扭矩到速度、位置、朝向的积分，并应用全局阻尼；基础碰撞检测仍为占位。
- **数学调用修正**：统一使用 `NCL::Maths::Vector` 命名空间下的 `Normalise/Length/LengthSquared`，解决 `VectorTemplate` 无成员报错。
- **项目文件**：新增 `ProjectPlan.md`（执行计划），完善 `.gitignore`（忽略 VS/CMake 构建产物）。

## 受影响的主要文件
- `CSC8503/TutorialGame.cpp` / `TutorialGame.h`
- `CSC8503CoreClasses/PhysicsSystem.cpp`
- `ProjectPlan.md`
- `.gitignore`

## 当前状态
- 玩家可受力移动，速度受限；物理积分可运行但碰撞解算仍占位，需后续实现。
- Gravity 默认可通过 G 键切换，重置世界时玩家引用、选中状态重置。

## 后续建议
1) 实现 BasicCollisionDetection / ImpulseResolveCollision，确保玩家与地面/关卡体正常碰撞并启用重力。
2) 添加跳跃/接地检测与阻尼/摩擦参数调优。
3) 按计划推进关卡与 AI/网络模块。
